[project]
name = "jarvis-contracts"
version = "0.1.0"
description = "Typisierte Verträge — einzige Quelle der Wahrheit für alle JARVIS-Module"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pydantic>=2.9,<3",
    "email-validator>=2.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["jarvis_contracts"]
